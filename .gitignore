# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# 环境变量（含真实 API key，绝不入库）
.env
*.local.env

# 运行时产物
langgraph_cs/data/chroma_rag/   # Chroma 向量库（embedding 产物，可由 ingest 脚本重建）
*.sqlite                        # SqliteSaver 检查点（多轮记忆运行时产物）
*.sqlite-wal
*.sqlite-shm
logs/

# 个人面试谈点，本地留底，不公开
docs/interview-prep.md

# 内部深度评审（含缺陷清单与面试策略），本地留底，不公开
docs/project-review.md

# 内部改进计划（工具化路线图），本地留底，不公开
docs/improvement-plan.md

# 给编码 agent 的任务书（系列），本地留底，不公开
docs/codex-task*.md

# superpowers 脚手架产物（brainstorm mockup 草稿、SDD 进度台账等，本地临时文件）
.superpowers/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
docs/superpowers/
reviews/

# 本机 Claude Code 配置与 worktree（settings.local.json、worktrees/ 等，不共享）
.claude/
