# langgraph_cs 本任务专属 .gitignore
#
# 仓库根的 .gitignore 有一条 `data/` 会把整个 langgraph_cs/data/ 忽略掉，
# 连本任务自建的 FAQ 语料（data/faq/*.md，核心交付物）也会被一并忽略、无法入库。
# 这里就地修正：重新纳入 data/ 与 data/faq/，但仍忽略向量库产物与本地缓存。

# 先把 data/ 目录重新纳入（否则其下文件无法被 !negation 救回）。
!data/
!data/faq/
# 但 Chroma 向量库是 embedding 产物，不入 git（体积大、可由 ingest 脚本重建）。
data/chroma_rag/
